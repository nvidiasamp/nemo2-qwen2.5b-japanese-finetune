# Task ID: 13
# Title: 集成WandB到推理演示流程
# Status: pending
# Dependencies: 12
# Priority: medium
# Description: 将WandB监控功能集成到推理脚本中，用于在现场演示时实时记录和展示每个推理请求的耗时、输入和输出，增强演示效果。
# Details:
修改 `scripts/05_run_inference.py`，添加WandB的初始化和日志记录功能。
```python
# ... imports ...
import wandb
import time

# ...
if __name__ == "__main__":
    wandb.init(project="qwen25-demo-live", entity="your-team")
    # ... (trainer, model_path, prompts 定义) ...
    start_time = time.time()
    results = api.generate(...)
    inference_time = time.time() - start_time
    
    wandb.log({
        "prompt": prompts[0],
        "response": results[0],
        "inference_time_sec": inference_time
    })
    print(results)
    wandb.finish()
```

# Test Strategy:
运行推理脚本后，WandB的`qwen25-demo-live`项目页面出现新的run，并记录了prompt、response和推理时间等信息。
